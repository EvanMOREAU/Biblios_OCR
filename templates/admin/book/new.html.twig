{% extends 'base.html.twig' %}

{% block title %}Ajout d'auteur
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="example-wrapper">
        <br><br>
		<h1>Ajout d'auteur</h1>

		{{ form_start(form) }}
            {{ form_row(form.title, {label: 'titre'}) }}
            {{ form_row(form.isbn, {label: 'ISBN'}) }}
            {{ form_row(form.cover, {label: 'Couverture'}) }}
            {{ form_row(form.editedAt, {label: 'Date édition'}) }}
            {{ form_row(form.plot, {label: 'Plot'}) }}
            {{ form_row(form.pageNumber, {label: 'Nombre de page'}) }}
            {{ form_row(form.status, {label: 'Status'}) }}
            {{ form_row(form.editor, {label: 'Editeur'}) }}
            {{ form_row(form.authors, {label: 'Autheur'}) }}



		    <button type="submit" class="btn btn-primary">Envoyer</button>
		{{ form_end(form) }}
	</div>
{% endblock %}
